{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "packageRules": [
    {
      "description": "Hold System.CommandLine on the 2.0.0-beta4 API. The 2.0 GA removed the entire fluent builder surface Scribegate.Cli is written against (~167 call sites across 6 CLI files), so bumping it fails the build. Re-enable once the CLI is migrated to the GA API.",
      "matchPackageNames": ["System.CommandLine"],
      "enabled": false
    },
    {
      "description": "Migrated off FluentAssertions to AwesomeAssertions (the free Apache-2.0 fork) because FluentAssertions v8+ is commercially licensed (Xceed) and Scribegate runs a commercial managed tier. Guard against any accidental reintroduction of FluentAssertions pulling in the commercial line.",
      "matchPackageNames": ["FluentAssertions"],
      "allowedVersions": "<8"
    },
    {
      "description": "@web/dev-server-core 0.7.x requires ws ^7, and package.json pins a scoped override to keep it on a patched v7. Forcing v8 through that override breaks the test server (PR #62). Cap it so only in-range v7 patches are offered. This override used to sit alongside a second one for jsdom on ws ^8; Renovate read the pair as one dep and proposed crossing them in both directions. jsdom 30 dropped its ws dependency entirely, so that override is gone and only this one remains.",
      "matchPackageNames": ["ws"],
      "matchCurrentVersion": "^7",
      "allowedVersions": "<8"
    },
    {
      "description": "katex is only ever loaded through marked-katex-extension (lib/katex-lazy.ts), and that package still declares a peer of `katex: >=0.16 <0.18` as of 5.1.10 — its latest. Taking katex to 0.18 therefore breaks the peer contract with the only thing that consumes it. Lift this cap once marked-katex-extension widens its range.",
      "matchPackageNames": ["katex"],
      "allowedVersions": "<0.18"
    }
  ]
}
